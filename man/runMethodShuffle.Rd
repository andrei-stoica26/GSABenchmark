% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shuffle.R
\name{runMethodShuffle}
\alias{runMethodShuffle}
\title{Run gene set analysis method on shuffled gene sets}
\usage{
runMethodShuffle(
  scObj,
  labelCol,
  geneSets,
  gsaMethod,
  loss = c(0, 0.2),
  noise = c(0, 0.2),
  doGrid = TRUE,
  seeds = c(1, 2, 3),
  outputFun = identity
)
}
\arguments{
\item{scObj}{A \code{Seurat} or \code{SingleCellExperiment} object.}

\item{labelCol}{The metadata column containing the ground truth annotation.}

\item{geneSets}{A named list of gene sets.}

\item{gsaMethod}{Name of the gene set analysis method.}

\item{loss}{A numeric vector of gene loss values. Must be in [0, 1).}

\item{noise}{A numeric vector of noise values. Must be in [0, 1).}

\item{doGrid}{Whether to run the methods for each loss-noise combination.}

\item{seeds}{A numeric vector of random seeds passed
to \code{hammers::shuffleGenes}. Its length determines the number of
replicates.}

\item{outputFun}{Choose between silently_run (suppress all warnings and
messages) or identity (do not suppress them). Default is silently_run.}
}
\value{
A \code{Seurat} or \code{SingleCellExpression} object
with the results of the runs stored as metadata columns.
}
\description{
This function runs a gene set analysis method on shuffled gene sets.
}
\examples{
scoPath <- system.file('extdata', 'scObj.qs', package='GSABenchmark')
scObj <- qs::qread(scoPath)
gsPath <- system.file('extdata', 'geneSets.qs', package='GSABenchmark')
geneSets <- qs::qread(gsPath)
scObj <- runMethodShuffle(scObj, 'label', geneSets, 'CSOA', 0.2, 0.2)

}
