% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_benchmark.R
\name{runBenchmark}
\alias{runBenchmark}
\title{Generate all benchmark results
This function performs the entire \code{GSABenchmark} pipeline.}
\usage{
runBenchmark(
  scObj,
  labelCol,
  geneSets,
  gsaMethods,
  runEFBenchmark = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{scObj}{A \code{Seurat} or \code{SingleCellExperiment} object.}

\item{labelCol}{The metadata column containing the ground truth annotation.}

\item{geneSets}{A list of gene sets. If not \code{NULL} while
\code{efBenchmark} is \code{NULL}, the efficiency benchmark will be run.}

\item{gsaMethods}{Character vector of gene set analysis methods.}

\item{runEFBenchmark}{Whether to run efficiency benchmark.}

\item{verbose}{Whether the output of the efficiency benchmark should be
verbose. Ignored if \code{runEFBenchmark} is \code{FALSE}.}
}
\value{
A list of benchmark results.
}
\description{
Generate all benchmark results
This function performs the entire \code{GSABenchmark} pipeline.
}
\details{
A wrapper around \code{allBenchmarkResults}. Slower for repeated
runs, but it does not require users to manually generate \code{normSilDF}
and \code{dimMat}.
}
\examples{
scoPath <- system.file('testdata', 'scObj.qs', package='GSABenchmark')
scObj <- qs::qread(scoPath)
gsPath <- system.file('testdata', 'geneSets.qs', package='GSABenchmark')
geneSets <- qs::qread(gsPath)
smr <- runBenchmark(scObj, 'label', geneSets, c('CSOA', 'Zscore'), FALSE)

}
